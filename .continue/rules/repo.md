# Premiere Pro MCP

Follow the repository root `AGENTS.md`. It is the canonical map of architecture, commands, ExtendScript rules, UXP vs CEP, verification language, telemetry, and claims.

Use Node.js 24 for repo work (engines floor 20.19). Install with `npm ci`. Finish with `npm run check`.
