# Junie guidelines

Follow `AGENTS.md` at the repository root. That file is the canonical map of architecture, commands, ExtendScript, UXP vs CEP, verification language, telemetry, and claims.

Use Node.js 24 for repo work (engines floor 20.19). Install with `npm ci`. Finish with `npm run check`. Automated tests do not prove live Premiere behavior.
