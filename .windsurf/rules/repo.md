---
trigger: always_on
---

# Premiere Pro MCP

Read and follow the repository root `AGENTS.md` before editing. It is the canonical map of architecture, commands, ExtendScript rules, UXP vs CEP, verification language, telemetry, and claims.

Use Node.js 24 for repo work (engines floor 20.19). Install with `npm ci`. Finish with `npm run check`. Automated tests do not prove live Premiere behavior.
